<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
         "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.house.model.dao.MemberFavoriteDao">
    <!-- 1. 즐겨찾기 추가 -->
    <insert id="insertFavorite">
    INSERT INTO member_favorite (mno, apt_seq)
    VALUES (#{mno}, #{aptSeq})
    </insert>

    <!-- 2. 즐겨찾기 삭제 -->
    <delete id="deleteFavorite">
    DELETE FROM member_favorite
    WHERE mno = #{mno}
      AND apt_seq = #{aptSeq}
    </delete>

    <!-- 3. 회원별 즐겨찾기 조회 -->
    <select id="selectFavoritesByMember" resultType="com.ssafy.house.model.dto.HouseInfo">
    SELECT hi.*
      FROM house_info hi
      JOIN member_favorite mf ON hi.apt_seq = mf.apt_seq
     WHERE mf.mno = #{mno}
    </select>

</mapper>
